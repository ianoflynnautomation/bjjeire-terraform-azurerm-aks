# workload-identities

Composition wrapper over the AVM user-assigned managed identity module,
applying it with `for_each` so the whole set of workload identities is
declared as one map.

Each entry carries its own federated identity credentials and Azure role
assignments, and the module returns `client_ids` and `principal_ids` maps that
the root module wires into Key Vault, storage, and GitHub.

The identities themselves and their trust subjects are documented in
[identity.md](../../docs/architecture/identity.md).

> Everything below `BEGIN_TF_DOCS` is generated by
> [terraform-docs](https://terraform-docs.io/). Run `pre-commit run terraform_docs`
> (or the command in [AGENTS.md](../../AGENTS.md)) after changing inputs or
> outputs — CI fails when it is stale.

<!-- BEGIN_TF_DOCS -->
## Requirements

| Name | Version |
| ---- | ------- |
| <a name="requirement_terraform"></a> [terraform](#requirement\_terraform) | >= 1.10.0, < 2.0.0 |
| <a name="requirement_azurerm"></a> [azurerm](#requirement\_azurerm) | ~> 4.57 |



## Modules

| Name | Source | Version |
| ---- | ------ | ------- |
| <a name="module_identity"></a> [identity](#module\_identity) | Azure/avm-res-managedidentity-userassignedidentity/azurerm | 0.5.2 |



## Inputs

| Name | Description | Type | Default | Required |
| ---- | ----------- | ---- | ------- | :------: |
| <a name="input_identities"></a> [identities](#input\_identities) | Map of workload identities to create. Key is a stable logical name (e.g. "api", "flux") used by callers to look up outputs.<br/><br/>- `name` - Final Azure name of the user-assigned identity.<br/>- `federated_identity_credentials` - FIC map forwarded to the user-assigned-identity primitive.<br/>- `role_assignments` - Role assignment map forwarded to the user-assigned-identity primitive. | <pre>map(object({<br/>    name = string<br/><br/>    federated_identity_credentials = optional(map(object({<br/>      audience = list(string)<br/>      issuer   = string<br/>      name     = string<br/>      subject  = string<br/>    })), {})<br/><br/>    role_assignments = optional(map(object({<br/>      role_definition_id_or_name             = string<br/>      scope                                  = string<br/>      condition                              = optional(string, null)<br/>      condition_version                      = optional(string, null)<br/>      delegated_managed_identity_resource_id = optional(string, null)<br/>      description                            = optional(string, null)<br/>      skip_service_principal_aad_check       = optional(bool, null)<br/>    })), {})<br/>  }))</pre> | n/a | yes |
| <a name="input_location"></a> [location](#input\_location) | Azure region for the identities. | `string` | n/a | yes |
| <a name="input_resource_group_name"></a> [resource\_group\_name](#input\_resource\_group\_name) | Resource group all identities are created in. | `string` | n/a | yes |
| <a name="input_enable_telemetry"></a> [enable\_telemetry](#input\_enable\_telemetry) | Enable AVM module telemetry (modtm provider) on every identity. | `bool` | `false` | no |
| <a name="input_tags"></a> [tags](#input\_tags) | Tags applied to every identity. | `map(string)` | `{}` | no |

## Outputs

| Name | Description |
| ---- | ----------- |
| <a name="output_client_ids"></a> [client\_ids](#output\_client\_ids) | Map of identity key => client ID. Used for ServiceAccount annotations and CI secret values. |
| <a name="output_ids"></a> [ids](#output\_ids) | Map of identity key => resource ID of the user-assigned identity. |
| <a name="output_names"></a> [names](#output\_names) | Map of identity key => Azure name of the identity. |
| <a name="output_principal_ids"></a> [principal\_ids](#output\_principal\_ids) | Map of identity key => principal (object) ID. Used for Azure RBAC role assignments. |
<!-- END_TF_DOCS -->
